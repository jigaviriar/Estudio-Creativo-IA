.env
app/data/
logs/
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# El .docx exportado es un derivado binario del mismo contenido de docs/;
# se mantiene local en vez de versionarlo.
*.docx

# Material del instituto con aviso de "reproducción prohibida" — no se redistribuye.
IEP/
